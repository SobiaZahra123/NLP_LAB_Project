# ── Core Web Framework ────────────────────────────────────────────────────────
streamlit==1.35.0

# ── Document Parsing ──────────────────────────────────────────────────────────
pdfplumber==0.11.1          # PDF text extraction (no Poppler binary needed)
python-docx==1.1.2          # DOCX / Word file parsing

# ── AI / NLP — TensorFlow backend ────────────────────────────────────────────
tensorflow-cpu==2.16.1      # TensorFlow (CPU-only, lighter for cloud deploy)
transformers==4.41.2        # HuggingFace Transformers (zero-shot + summarizer)
tokenizers==0.19.1          # Fast tokenizers (required by transformers)
sentencepiece==0.2.0        # Tokenizer for many HF models

# ── Numerical / Utility ───────────────────────────────────────────────────────
numpy==1.26.4
